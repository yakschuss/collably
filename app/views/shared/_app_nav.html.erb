<nav id="insideNav" class="navbar navbar-default navbar-fixed-top dash-nav">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="container-fluid">

    <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <%= link_to "Collably", root_path, class: "navbar-brand page-scroll" %>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
  <ul class="nav nav-tabs">
    <div class="nav navbar-nav navbar-left">
        <% if current_user && current_user.first_name != "" %>
      <li>
        <%= link_to "#{current_user.first_name}'s Dashboard", user_path(current_user), class: 'navbar-link' %>
      </li>
        <% elsif current_user %>
        <li>
          <%= link_to "Dashboard", user_path(current_user), class: 'navbar-link' %>
        </li>
        <% end %>
    </div>
    <div class="nav navbar-nav navbar-right">
      <% if current_user %>
              <% if current_user.unread_messages? %>
            <li>
              <%= link_to mailbox_inbox_path do %>
                <p>Inbox <span class="badge"> <%= "#{unread_messages_count}"%></span></p>
              <% end %>
            </li>
              <% else %>
            <li>
              <%= link_to "Inbox", mailbox_inbox_path %>
            </li>
              <% end %>
        <li>
           <%= link_to 'Edit profile', edit_user_registration_path, class: 'navbar-link' %>
        </li>
        <li>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'navbar-link'  %>
        </li>
      <% else %>
        <li>
          <%= link_to "Sign In", new_user_session_path, class: 'navbar-link'  %>
        </li>
        <li>
          <%= link_to "Sign Up", new_user_registration_path, class: 'navbar-link'  %>
        </li>
      <% end %>
      </ul>
    </div>
  </div>
