<%= form_for @event do |f| %>

      <%= f.label :title %>
      <%= f.text_field :title, class: 'form-control', placeholder: "Enter Event Title" %>
<br>
      <%= f.submit "Save", class: 'btn btn-success' %>

    <% end %>
