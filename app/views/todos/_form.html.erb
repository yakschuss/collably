<%= form_for [event, todo], remote: true do |f| %>
    <div class="form-group col-md-2">
      <%= f.text_field :name, class: 'form-control', placeholder: "Enter Task" %>
    </div>
      <%= f.submit "Save", class: 'btn btn-success' %>
  <% end %>
